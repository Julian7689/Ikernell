'use client';
import { BorderBeam } from '@/components/magicui/border-beam';
import { motion } from 'framer-motion';

export default function Space3() {
    return (
        <div className="transform -translate-x-10 -translate-y-20 w-[680px] h-[340px] bg-[#232325] rounded-2xl">
            {/* Contenido aquí */}
        </div>
    );
}